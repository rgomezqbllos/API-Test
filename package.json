{
  "name": "api-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "test:report": "npm run normalize:collection && npm run test:newman",
    "normalize:collection": "node scripts/normalize-collection.cjs",
    "test:newman": "newman run collections/Integration-consumer.postman_collection.normalized.json -e envs/Dev.postman_environment.json --iteration-data data.json --reporters cli,htmlextra,json --delay-request 50"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}

